1. Архитектура CNN

- 3 сверточных блока (32→64→128 фильтров)
- Batch Normalization для стабильности
- Dropout для предотвращения переобучения
- Полносвязные слои для классификации

2. Загрузка данных
Ожидаемая структура папок:
```
data/
  ├── 0/  (100 PNG изображений цифры 0)
  ├── 1/  (100 PNG изображений цифры 1)
  ├── ...
  └── 9/  (100 PNG изображений цифры 9)
```
3. Обучение

- Автоматическое разделение на train/validation/test
- Early stopping (останавливается, если не улучшается)
- Уменьшение learning rate при застое
- Сохранение лучшей модели

4. Визуализация

- График обучения (точность и потери)
- Матрица ошибок (confusion matrix)
- Примеры предсказаний с уверенностью

5. Использование модели
```python
# После обучения
model = keras.models.load_model('model_results_XXXXX/best_model.keras')
predict_image(model, 'my_drawing.png')
```
Что нужно сделать:

1. Установите библиотеки:

```bash
pip install tensorflow pillow matplotlib scikit-learn seaborn
```

2. Подготовьте данные в структуре папок выше
3. Запустите скрипт:

```bash
python digit_classifier.py
```
Модель будет работать с вашими цветными 64×64 изображениями, включая те, что с миксовыми цветами! 🎨🧠RetryClaude can make mistakes. Please double-check responses. Sonnet 4.5
